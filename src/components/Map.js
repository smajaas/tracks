import React from 'react';
import { Text, StyleSheet, View } from 'react-native';


const Map = () => {
    return (
        <View></View>
    )
}

const styles = StyleSheet.create({})

export default Map;